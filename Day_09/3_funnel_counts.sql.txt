-- Conversion funnel (view → cart → purchase example)
SELECT event_type, COUNT(*) AS event_count 
FROM delta.`dbfs:/Volumes/workspace/ecommerce/silver/events_delta` 
GROUP BY event_type ORDER BY event_count DESC;